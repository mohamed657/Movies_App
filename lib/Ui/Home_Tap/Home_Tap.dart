import 'package:flutter/material.dart';

class Home_Tap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigoAccent,
    );
  }
}
